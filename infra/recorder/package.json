{
  "name": "flowsmartly-recorder",
  "version": "1.0.0",
  "private": true,
  "description": "FlowSmartly Training Room recording bot (headless Chrome + ffmpeg screen capture)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "puppeteer": "^23.10.0"
  }
}
